@model IEnumerable<Segmento.Domain.TwitterReportLikesByPeriod>

@{
//Layout = null;
}

<!DOCTYPE html>

<html>
<head>
	<meta name="viewport" content="width=device-width" />
	<title>Index</title>
</head>
<body>
	<hr />
	<hr />
	<table class="table">
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.UserName)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.TweetsCount)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.LikesMedian)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.BestTimePeriod)
			</th>			
			<th>
				@Html.DisplayNameFor(model => model.LastUpdateDate)
			</th>
			
			<th></th>
		</tr>

		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.ActionLink(item.UserName, "Details", "ReportLikesByPeriod", new { id = item.ID })
					@*@Html.DisplayFor(modelItem => item.UserName)*@
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.TweetsCount)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.LikesMedian)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.BestTimePeriod)
				</td>
				
				<td>
					@Html.DisplayFor(modelItem => item.LastUpdateDate)
				</td>
			</tr>
		}

	</table>
</body>
</html>
